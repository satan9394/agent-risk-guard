# Node
node_modules/
dist/
build/
*.tsbuildinfo

# 本地运行/日志
*.log
.tmp/
coverage/

# Real Agent Conformance evidence（生成物；由 scripts/run-real-conformance.ts 按需落盘）
tests/evidence/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# 本机敏感/临时（绝不入库）
*.key
.editorconfig.local

# --- security hardening (added 2026-09-18) ---
# env / secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
id_rsa
id_ed25519
.netrc
.npmrc.local
.pypirc
secrets.json

# --- DSH 任务台账过程产物（2026-09-18 加入）---
# 这些是评估/变异测试/基线快照/探针输出等一次性过程文件，
# 不属于仓库内容；每次迭代都会重新生成，纳入版本控制只会污染 git status。
# 注意：tasks/orchestrator/ 下的 DECISIONS.md、EVALUATION_RESULT_*.md 等
# 正式结论文件不以下划线开头，不受影响，仍正常跟踪。
tasks/orchestrator/_*
tasks/eval-g*/
tasks/**/*.tmp
.eval-tmp/
