# 开发日志：v0.3.0 — Real Agent Conformance（2026-09-07）

> 前置：v0.2.2（ACS Protocol Finalization，version gate + release assets）见 `docs/devlog-2026-09-05-v0.2.2.md`。
> 执行任务书：《Agent Risk Guard v0.2.2 收尾 + v0.3.0 Real Agent Conformance 执行任务书.md》Phase B。
> 基线：v0.2.2 已冻结 ACS 协议层（§十三），核心转向真实 Agent 会话验证。

## 本轮目标（Phase B：v0.3.0 Real Agent Conformance）

从「构建基础设施」转向「真实 Agent 会话验证」：

1. **5 Agent Conformance**——OpenCode / Claude Code / DSH / AGY 拿到真实会话 D3 硬拦截证据；Codex 应用形态拿到真实会话拦截确认。
2. **GAN 对抗审查闭环**——5-Agent 判别器审查 findings 全部修复。
3. **installer UX**——`detect` 全量 Agent registry + 交互式安装选择。
4. **agy adapter**——首个完整 D3 全链路样板。
5. **ps1 / 插件事务资产入仓库**——`assets/` 作为单一规则源分发。

## 交付内容

### 1. 5 Agent Real Agent Conformance（docs/real-agent-conformance-final-report.md）

| Agent | 版本 | 机制 | 拦截证据 | windows D 等级 | 说明 |
|---|---|---|---|---|---|
| **OpenCode** | 1.18.29 | `tool.execute.before` 插件 | 真实会话 `git reset --hard` 被 `BLOCKED_BY_GLOBAL_SAFETY_GUARD/GIT_RESET_HARD` 拒绝，git status 仍 M file.txt，副作用保留 | **D3** | fail-closed |
| **Claude Code** | settings.json 接线 | PreToolUse hook | 真实会话 `git reset --hard` 输出 `permissionDecision=deny`（RG-GIT-001），permission-rule 拒绝执行、未提交改动存活 | **D3** | fail-closed |
| **DSH** | 当前 web profile | `pre-execute` 瀑布 + `guard()` | D2 源码实证 + D3 真实会话拦截（黑名单词命令被门禁 pre-execute 拒绝，副作用未发生） | **D3** | fail-closed |
| **AGY** | 1.1.27 | PreToolUse hook（hooks.json, run_command matcher）+ 双脚本 | 真实会话尝试 `git reset --hard HEAD` → hook deny（"tool call denied"），未提交修改保留 | **D3** | fail-closed |
| **Codex** | 0.146.x（应用形态） | hooks.json + config.toml `[[hooks.PreToolUse]]` 双注册 | 2026-09-06 应用内真实会话拦截确认（永久删除被拒、文件保留） | **D3**（应用形态） | **拦截来源 = Codex 应用自身 `approval_policy=never` + `sandbox=unelevated` 策略/沙箱层**；RiskGuard hook 应用会话触发待补测 |

**Codex 应用形态真相（本轮关键如实结论）**：本机 Codex 为应用形态（VS Code 扩展 openai.chatgpt + 本地 server codex.exe），
机器级 hook 双注册（Claude Code 均不适用，Codex 走 hooks.json + config.toml）。doctor dynamic PASS（真实 spawn hook
+ 无害 allow + 危险 deny self-test），DENY `{decision:'deny'}` exit 2、ALLOW `{}` exit 0。2026-09-06 用户应用内手动验证：
创建测试文件 → 尝试永久删除被拒（返回 `blocked by policy`，无具体原因）、文件保留。但 **`%TEMP%\riskguard-hook-calls.log`
无该次测试触发的 hook deny 记录** → 拦截来自 Codex 应用自身 `approval_policy=never` + `sandbox=unelevated` 策略/沙箱层，
**RiskGuard hook 双注册在应用会话的触发尚未确认**，诚实标注并待应用会话补测。D3 标注为「应用形态真实会话拦截确认」，
不代表 RiskGuard hook 在应用会话已实锤触发。

### 2. GAN 对抗审查闭环（17/17 findings 修复）

workflow fan-out 4 个独立判别器（纯静态源码分析）。findings = **P0×10 / P1×6 / P2×1 = 17 条**，
落在共享 ps1（6）/ OpenCode 插件（4）/ AGY 适配器（3）/ DSH YAML（4），**全部修复**：

| 落点 | 修复向量 | 修复提交 |
|---|---|---|
| 共享 ps1（CC/Codex/AGY 共用） | 大小写绕过、fail-open、落盘执行、插词、包装/换名变体、ri/echo del 误伤 | T4a：改主源 `dangerous-commands.ps1` + 同步三处生产，BOM 保持 |
| OpenCode 插件 | arm 误排、前置词/反斜杠/插词/变量拼接、`-xec` 组合解包、os.system/os.popen 错位 | T4b：`2159afd` + 生产 29501B，哈希一致 |
| AGY 适配器 | EncodedCommand/base64、引号包裹命令名、git restore/checkout 单文件 | T4c：加固适配器 + 复用共享规则源 |
| DSH YAML | 拆拼/变量拼接/转义/base64 管道、管道/xargs/-execdir 落空、工具名匹配盲区、单点依赖+fail 语义 | T4c：`2159afd` + T4d：`7fe08a8`（R4 规则同步 `defaultDenyRules`，deploy.ts +29） |

单一规则源纪律：ps1 主源一处，三处生产（~/.claude/hooks、~/.codex/hooks、~/.gemini/config/hooks）哈希一致；
opencode 生产与 monorepo assets 哈希一致。合法命令（git status / ls / echo hello / ri Array#map）不误伤。

### 3. installer UX（T7）

- `AGENT_REGISTRY` 补 agy 条目（~/.gemini config PreToolUse(run_command) hook）。
- `cmdDetect`：遍历全量 registry（含 agy），`--json` 返回完整映射。
- `cmdInstall`：无 `--agent` → detect + 交互式编号选择（`1,3` / `all` / Enter）via readline；
  非 TTY / 管道 / 超时 / EOF → 回退默认全装（绝不挂起）。
- `--agent <id>` 精确选择保留；`--yes` / `--all` 跳过提示。
- 新增 `tests/e2e/install-ux.test.ts`（16 例）；`lifecycle.e2e.test.ts` detect --json 更新为全量映射。

### 4. agy adapter（新增）

`packages/adapters/agy/src/index.ts` + `tests/adapter/agy-adapter.test.ts` + `compatibility.json` 补 agy D3 条目。
全局 hooks `~/.gemini/config/hooks.json`（PreToolUse run_command）→ `agy-dangerous-commands.ps1`（BOM、fail-closed）
→ 共享规则源 `dangerous-commands.ps1`。**hook command 必须绝对路径**（相对路径子目录启动 127 绕过，已文档提醒）。
agy 是第一个把「真实会话拦截证据 + 双脚本（适配器+共享规则）+ D3 标注」全链路闭合的 agent。

### 5. ps1 / 插件事务资产入仓库

- `assets/hooks/dangerous-commands.ps1`（GAN-hardened 21466B，单一源同步 CC/Codex/AGY 生产）。
- `assets/hooks/agy-dangerous-commands.ps1`（BOM 加固适配器）。
- commit `93551c4` vendor 入库，monorepo 作为 scripts 规则单一事实源分发。

### 6. 版本 0.2.2 → 0.3.0

- `package.json` / `package-lock.json`（2 处）/ `packages/acs/package.json` / `packages/installer/compatibility.json` productVersion → `0.3.0`。
- `tests/product/product.test.ts` 断言 productVersion → `0.3.0`。
- CHANGELOG：新增 `[Unreleased] v0.3.0 Real Agent Conformance`；`[0.2.2]` 转历史（已发布）。
- README：状态 → v0.3.0 Developer Preview，定位补 Real Agent Conformance；文档导航加 v0.3.0 devlog。
- `docs/generated/agent-security-matrix.md` 重新生成（product 0.3.0）。

## 验证

- **全量 platform-independent 测试**：`node --test` ci.yml 全套 → **312/312 全绿**（含更新后的 product 断言）。
- `node scripts/generate-agent-security-matrix.ts --check`：矩阵与 source 一致（OK）。
- `node scripts/build-release.ts`：v0.3.0 artifact 重建（待 release workflow 上传）。

## Release Gate（v0.3.0）

- [x] 版本统一 0.3.0（package/lock/acs/compatibility/product 断言/README）
- [x] 5 Agent D3 证据（OpenCode/CC/DSH/AGY 真实会话；Codex 应用形态，hook 待补测）
- [x] GAN 17/17 findings 修复
- [x] installer UX（detect 全量 + 交互式选择）
- [x] agy adapter 新增
- [x] ps1 / 插件事务资产入仓库
- [x] docs/generated/agent-security-matrix.md 重新生成且 --check 通过
- [x] 全量测试 312/312 全绿
- [x] 提交推送 main，tag v0.3.0 推送 origin
- [ ] release.yml verify(Node 22/24) + release job success
- [ ] `gh release view v0.3.0` assets 含 `agent-risk-guard-v0.3.0.tar.gz` + `SHA256SUMS.txt`，prerelease=true

## 遗留与后续

- **Claude Code 持久性教训**：settings.json 的 PreToolUse 曾外部还原丢失 → 建议 doctor 巡检 + 事务式重装立为日常治理动作，hash 加入 CI 校验或启动探针。
- **monorepo adapter F3 缺口**：core `unwrapShellWrapper` 不覆盖 `-xec` 组合短参解包（已 opencode 插件层闭合，core 层待补齐通用解包装）。
- **Copilot CLI / Windsurf / Cursor**：doctor SKIP，adapter 已实现（Copilot D2 / Windsurf D2 / Cursor D2），真实 D3 待本机安装；Copilot preToolUse deny 部分版本回归（issue #3874）→ failMode unknown，不标 D3。
- **Codex 应用形态**：RiskGuard hook 双注册在应用会话的触发待补测。