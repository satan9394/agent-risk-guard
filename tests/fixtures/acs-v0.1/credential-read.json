{
  "_expected": "allow",
  "_note": "credentials.read → credential_read；Profile B 当前策略放行读取（RG-CRED-001 只 deny 导出 credential_export）。诚实记录现状，非营销声明。",
  "tool": { "name": "credentials", "provider": "cli-smoke" },
  "operation": "read",
  "capability": "credentials.read",
  "arguments": { "path": "~/.ssh/id_ed25519" },
  "intent": { "description": "check key presence" }
}
